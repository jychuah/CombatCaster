import { InitiativePipe } from './initiative.pipe';

describe('InitiativePipe', () => {
  it('create an instance', () => {
    const pipe = new InitiativePipe();
    expect(pipe).toBeTruthy();
  });
});
